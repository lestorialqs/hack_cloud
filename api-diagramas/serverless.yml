service: api-hack-diagramas

frameworkVersion: '3'

provider:
  name: aws
  runtime: python3.13
  timeout: 30  
  iam:
    role: arn:aws:iam::708642711016:role/LabRole
  stage: dev
  region: us-east-1

  
functions:
  crearJson:
    handler: CrearJson.lambda_handler
    events:
      - http:
          path: diagramas/crearJson
          method: post
  
